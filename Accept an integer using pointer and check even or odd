#include <stdio.h>

int main()
{
    int num;
    int *p;

    p = &num;

    printf("Enter a number: ");
    scanf("%d", p);

    if(*p % 2 == 0)
        printf("Number is Even\n");
    else
        printf("Number is Odd\n");

    return 0;
}
